## 절대 원칙 (위반 금지 — 다른 모든 원칙보다 우선)
- 모든 작업(조회/수정/커밋/빌드/실행/검증)은 WSL /root/yp_parity/yp_project 에서만 수행한다.
- Windows 경로(C:\Users\...)는 절대 직접 읽거나 쓰지 않는다 (git -C 윈도우경로, Read/Edit 윈도우경로 등 금지). 해당 경로는 삭제 예정이라 동기화 대상도 아님.
- 위반 시 코드 꼬임/양방향 충돌 → 이 원칙은 최우선.

사용자 인터페이스 텍스트/라벨/문구/버튼/디자인은 별도 요청이 없는 한 절대 변경 금지.

작업은 최소 변경 원칙 (요청된 기능에 필요한 것만).

작업 전 변경 계획 제시, 완료 후 diff 요약 제공.

## 서버 운영 원칙 (중요)
- **양평서버(프로덕션, 3.38.80.64 / unocum.kr) 수정·배포·SSH 직접조작은 무조건 사용자 본인이 직접 수행한다.**
- assistant(에이전트)는 로컬 코드·로컬 Docker만 작업하며, 양평서버에는 절대 SSH/배포/직접수정을 하지 않는다.
- 양평서버 관련 변경(마이그레이션 upgrade, 배포, 재기동 등)은 사용자가 직접 하거나 명시적으로 위임할 때만 진행한다.

## WSL 작업 원칙 (작업 위치·범위·배포)

### 1. 작업 위치: WSL 패리티 복사본만 수정
- **단일 소스는 WSL**: `/root/yp_parity/yp_project`. 모든 코드 수정은 이곳에서만 한다.
- **C: 원본은 제외**: `C:\Users\i0wil\OneDrive\바탕 화면\project\yp_project`(Windows)는 디스크 용량 부족으로 삭제 예정이라 수정·동기화·커밋 대상에서 제외. C: ↔ WSL 양방향 동기화도 하지 않는다.
- IP `https://172.26.38.72`는 WSL의 Vite dev 서버(127.0.0.1:5173)를 proxy → WSL 수정이 브라우저 미리보기에 실시간 반영됨.

### 2. 배포: 무조건 사용자 본인 수행
- **프로덕션(양평서버) 배포는 사용자가 직접 한다.** assistant는 배포 절차·명령어만 제공하고, SSH/`deploy.sh`/docker 조작은 절대 하지 않는다.
- 배포 전 로컬 패리티 검증(빌드·구동·ingress 200)도 사용자가 확인.

### 3. 커밋 (WSL, dev 브랜치)
- 루트 `/root/yp_parity/yp_project`, 브랜치 `dev`.
- 비밀·산출물 커밋 금지: `.env_b`, `uploads_*.tgz`, `deploy/`, `backup_*.dump`, `*.tgz` 등. → `git add -u`(수정된 추적 파일만 스테이징) 권장. `git add .` 사용 금지.
- 커밋 전 `git status`/`git diff`로 대상 확인.

### 4. 검증 (WSL에서만)
- 변경 모듈이 Vite로 정상 서빙되는지: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:5173/src/...경로` (200 기대).
- 브라우저(IP) 강력 새로고침으로 UX 확인. 프로덕션 반영 전 빌드 필요: `cd frontend && npm run build`(dist는 배포 시 서버 컨테이너로 복사).

### 5. 공통 원칙 (유지)
- UI 텍스트/라벨/문구/버튼/디자인은 별도 요청 없는 한 절대 변경 금지.
- 최소 변경(요청 범위만). 작업 전 계획 제시, 완료 후 diff 요약.
- 배포 절차 상세는 `DEPLOY.md`, 빠른 배포는 `deploy.sh` 참고(모두 사용자 실행).