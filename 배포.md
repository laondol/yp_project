# 배포 절차

이번 변경분(자동보관 체계 + 안내 쪽지 + 관리자 draft 우선 정렬)을 양평 서버에 반영하는 순서입니다.

## 변경 내용 요약

- 공유 접수 시 10초 카운트다운/자동 발송 폐기 → **촬영/그리기 즉시 자동보관(draft)**, "공유 접수하기" 버튼으로 심사(review) 전환
- 자동보관이 생성되면 **파일이 실제 존재하는지 확인 후 작성자 회원에게 안내 쪽지를 1회 자동 발송**
- 관리자 공유마당 페이지에서 **📨 쪽지 버튼**으로 수동 재발송 가능 (관리자/책임자/심사승인 마을지기)
- 관리자 공유 목록은 **자동보관(draft)이 맨 앞에** 표시 (기본 탭도 '자동보관')
- 승인/공개 시 `created_at` 갱신 → 공유마당 맨 앞에 표시
- 파일 저장 경로: `share_reports/draft/`(보관) → `review/`(심사) → `approved/` or `rejected/`
- 반려 30일 경과 글은 `server_local/rejected_archive/YYYY-MM/` 자동 이동

---

## 사전 준비

- 이번 커밋: `c208c08`
- **프론트엔드는 반드시 최신 빌드 필요** (`frontend/dist`는 git에 안 올라가므로 아래 순서로 빌드·복사)
- DB는 별도 마이그레이션 불필요 (`run.py`가 부팅 시 자동으로 컬럼 추가:
  `share_report.auto_sent`, `share_report.rejected_at`, `user.share_mod_approved`)

---

## 배포 순서 (로컬 노트북에서 실행)

### 1. 프론트엔드 최신 빌드

```bash
cd "C:\Users\i0wil\OneDrive\바탕 화면\project\yp_project\frontend"
npm run build
```

> `dist/`에 최신 빌드가 생성됩니다. (이미 빌드되어 있으면 생략 가능)

### 2. 코드 푸시 (이미 커밋된 상태라면 push 만 수행)

```bash
cd "C:\Users\i0wil\OneDrive\바탕 화면\project\yp_project"
git push origin dev
```

### 3. 서버에서 코드 가져오기

```bash
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "cd /home/ubuntu/yp_project && git pull origin dev"
```

> 코드는 컨테이너 볼륨(`${.}:/yp_project`)으로 자동 반영되므로 Flask 재시작 불필요.

### 4. 프론트엔드 빌드 결과 반영 (중요!)

```bash
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "docker cp /home/ubuntu/yp_project/frontend/dist/. yp_flask:/yp_project/frontend/dist/"
```

### 5. 자동보관 워커 컨테이너 기동 (신규 서비스, 최초 배포 시)

`docker-compose.yml`에 `moderation_worker`가 추가됐습니다. 서버에서 해당 서비스만 올립니다.

```bash
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "cd /home/ubuntu/yp_project && docker compose up -d --build moderation_worker"
```

> 워커는 `review/` 폴더의 글을 2초 주기로 AI 검토 → 자동 승인/보류 처리합니다.
> **이 서비스가 없으면 자동보관된 글이 영원히 draft에 남습니다.**

### 6. 서버 `server_local/` 디렉토리 확인 (반려 아카이브용)

워커가 반려 30일 경과 글을 `server_local/rejected_archive/YYYY-MM/`로 이동합니다.
디렉토리가 없으면 워커가 자동 생성하지만, 안전하게 미리 만들어 둡니다.

```bash
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "cd /home/ubuntu/yp_project && mkdir -p server_local/rejected_archive && echo ok"
```

### 7. DB 자동 컬럼 추가 확인

Flask 부팅 시 `run.py`가 컬럼을 자동 추가하므로 특별히 할 일이 없습니다.
확인이 필요하면:

```bash
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "docker exec yp_postgres psql -U yp_dev -d yp_local -c \"\\d share_report\" | grep -E 'auto_sent|rejected'"
```

`auto_sent`, `rejected_at` 컬럼이 보이면 OK.

---

## 배포 후 검증

```bash
# 1. 사이트 응답 확인
curl -s -o /dev/null -w "%{http_code}\n" https://www.unocum.kr

# 2. Flask 로그 (자동보관 API 호출 시)
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "docker logs yp_flask --tail 50"

# 3. 워커 로그 (AI 검토 확인)
ssh -i "C:\Users\i0wil\.ssh\LightsailDefaultKey-ap-northeast-2_U .pem" ubuntu@3.38.80.64 "docker logs yp_moderation_worker --tail 50"
```

## 테스트 시나리오

1. **회원 로그인 → 공유마당 → 촬영** → 즉시 `draft` 자동보관 + 안내 쪽지 수신
   - 쪽지가 실제로 오는지 편지함 확인
   - 페이지 이탈/위치 미입력 시에도 보관됨
2. **관리자 접속 → 공유마당 관리** → 처음 화면에 자동보관(draft)이 맨 위에 노출
3. **📨 쪽지 버튼** → 작성자에게 안내 쪽지 발송
4. **작성자가 '공유 접수하기'** → review 전환 → 워커 AI 검토 후 승인/보류
5. **승인된 글**이 공유 목록 최상위에 노출

## 주의사항

- 비회원은 회원 ID(user_id=0)이므로 안내 쪽지 대상을 **회원만** 발송 (파일에 존재 확인까지)
- `/share-report/notify/<id>`는 관리자/책임자만, 파일 존재 시에만 발송
- edit 권한(`share_mod_approved`)이 있는 마을지기도 공유 관리를 열 수 있으나, 쪽지 발송은 `_share_mgr()` 권한에 따름